---
services:
  dozzle:
    image: ghcr.io/amir20/dozzle:latest
    container_name: dozzle
    hostname: dozzle
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    env_file: .env
    volumes:
      - /opt/stacks/dozzle/config:/data:rw
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - 8080:8080/tcp
    networks:
      - proxy

networks:
  proxy:
    external: true
