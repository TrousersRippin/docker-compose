---
services:
  mkvtoolnix:
    image: ghcr.io/jlesage/mkvtoolnix:latest
    container_name: mkvtoolnix
    hostname: mkvtoolnix
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - TZ=Europe/Stockholm
      - PUID=1000
      - PGID=1000
      - DARK_MODE=1
    volumes:
      - /opt/stacks/mkvtoolnix/config:/config:rw
      - /mnt/ssd:/storage:rw
    ports:
      - 5800:5800/tcp
    networks:
      - proxy

networks:
  proxy:
    external: true
